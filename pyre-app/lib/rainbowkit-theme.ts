import { darkTheme } from "@rainbow-me/rainbowkit";

/* PYRE brand tokens mapped onto RainbowKit's dark theme. */
export const pyreTheme = darkTheme({
  accentColor: "#f0a93b",
  accentColorForeground: "#0b0a09",
  borderRadius: "medium",
  fontStack: "system",
});
