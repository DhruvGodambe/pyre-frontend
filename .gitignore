# Dependencies
node_modules/
**/node_modules/

# Build output
.next/
**/.next/
out/
build/
*.tsbuildinfo

# Secrets / env — never commit (keep .env.example templates)
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example

# Vercel
.vercel/
**/.vercel/

# Local data stores
.data/
**/.data/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Raw asset drops (SwissTransfer dumps etc.) — large binaries, kept local only.
# Shipped art is already optimized under pyre-app/public/.
swisstransfer_*/
**/swisstransfer_*/

# Raw designer deliveries at repo root (per-building source PNGs/MP3s + bundles).
# The optimized, in-use versions live under pyre-app/public/world/.
/bonfire/
/forge/
/vault/
/observertory/
/grand_exchange/
/black_market/
/hall/
/tavern/
/gate/
/Soundtrack/
/Character_PFP/
/UI_Elements/
/Other/

# ElevenLabs test clips (free-tier, non-commercial, never ship)
pyre-app/public/voice-previews/

# Announcement film working files: 432MB of raw takes, keyframes and renders.
# The finished film belongs in the app (public/intro), not in git history.
marketing/video/announcement/
